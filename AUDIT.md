# GIGA Standard v5 監査：けいさんカード

- 対象：`GIGAyama/Keisan-Card`（A型＝単一HTML完結／ビルドあり・自己ホスト）
- 監査日：2026-08-04
- 監査前の版：`c2ae79a`
- 実測の道具：`npm run check`（静的）／`npm run measure`（実ブラウザ・Chromium）

**このファイルの読み方**
「未計測」は ✅ ではありません。測っていないものは 未計測と書いています。
数字は すべて 実際に測った値です。推測で書いた数字は ひとつも ありません。

---

## 0. いちばん大事なこと（P0.5）

**改修前は、学校のネットワークで CDN が塞がれていると 画面が出ませんでした。**

これは推測ではありません。`c2ae79a` を実際に取り出し、外へ出られない状態
（学校のフィルタリングと同じ状態）で ひらいて 確かめた結果です。

```
外へ出ようとして 遮られたもの:
   https://unpkg.com/react@18/umd/react.production.min.js
   https://unpkg.com/react-dom@18/umd/react-dom.production.min.js
   https://unpkg.com/@babel/standalone/babel.min.js
   https://cdn.tailwindcss.com/
   https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New:...
JS エラー:
   ReferenceError: tailwind is not defined
   ReferenceError: Babel is not defined
画面に出ているもの: "よみこみ中…"
判定: ★ 画面が出ない
```

児童からは「壊れている」としか見えません。しかも **原因はアプリの外にある**ので、
先生が調べても 分かりません。

改修後は 同じ状態で **ふつうに起動します**（すべての画面で JS エラー 0件）。

### ブラウザに送るもの（実測）

| | 前 | 後 |
|---|---:|---:|
| `@babel/standalone`（ブラウザの中で JSX を翻訳） | **3,067.5 KB** | 0 |
| React ＋ ReactDOM（unpkg → 自己ホスト） | 139.2 KB | 139.2 KB |
| アプリ本体 | `App.jsx` 94.7 KB（**毎回コンパイル**） | `js/app.js` 52.5 KB（翻訳ずみ） |
| Tailwind | `cdn.tailwindcss.com`（ブラウザの中で CSS を生成・**未計測**） | `css/style.css` 34.4 KB |
| **CDN から取る実行コード** | **3本** | **0 バイト** |
| 初回に読むもの 合計 | **3,301.4 KB 以上**（Tailwind CDN を除いた実測値） | **246.5 KB** |

> Tailwind CDN の転送量だけは **未計測**です。この作業環境から
> `cdn.tailwindcss.com` へ出られないため、実バイトを取得できませんでした。
> そのぶんを除いても 3.3MB 以上あり、後の 246.5 KB と比べる分には足ります。

---

## 1. 実ブラウザでの実測（前 → 後）

**測り方**：3つの画面幅（320×568／375×667／1366×768）× 13の画面 ＝ **39通り**を
ひとつずつ ひらいて 測りました。件数は「39通りの中で見つかった のべ件数」です。
Google Fonts をふくむ 外部への通信は すべて遮った状態で測っています
（＝ 学校のフィルタリングと同じ状態）。

| 項目 | 前 | 後 |
|---|---:|---:|
| **コントラスト基準未満** | **110件**（要素として **77個**） | **0件** |
| **タップ領域 44px 未満** | **112件**（要素として **4個**） | **0件** |
| 320px 幅での横スクロール | 0件 | 0件 |
| **JS エラー** | **2件** | **0件** |
| CSP 違反 | （CSP なし） | **0件** |
| PWA の挙動 | 後述 | **0件** |

### コントラスト：とくにひどかったもの（白地・実測値）

| どこ | 色 | 比 | 直しかた |
|---|---|---:|---|
| **「はじめる」ボタン**（主役の入口） | 白 on `amber-500` | **2.15** | 面はそのまま、文字を `slate-900` に（7.3） |
| カレンダーの「ど」（土曜） | `sky-400` | **2.14** | `sky-700`（5.9） |
| カレンダーの日付・曜日 | `slate-400` | **2.56** | `slate-600`（7.6） |
| 「まだ きろくが ありません」（空状態） | `slate-300` | **1.48** | `slate-600`（7.6） |
| フッターの「GIGA山」 | `amber-600` | **3.19** | `amber-700`（5.0） |
| 「けす」ボタン | 白 on `rose-500` | **3.67** | 面を `rose-600` に（4.70） |
| 「やめる」ボタン | `slate-500` on `slate-100` | **4.34** | `slate-600` |
| タイトルの説明文 | `slate-500` on `#f6f7f9` | **4.44** | `slate-600` |

§2-8 のとおり、**いちばん読みにくかったのは 案内文と空状態**でした。
「まだ きろくが ありません」は 比 **1.48**。まだ記録が無い＝はじめたばかりの子に
向けた文が、いちばん読めない形になっていました。ベストタイムが無いときの
`--:--` も 同じ **1.48** です。

そして いちばん大きいのがこれです。

| どこ | 色 | 大きさ | 比 |
|---|---|---:|---:|
| **みどりカードの「11 － 2」**（児童が読む 主役そのもの） | 白 on `emerald-500` | **99.8px** | **2.54** |

99.8px の大きな文字は 3:1 で足りるのに、それにも **届いていませんでした。**
このアプリで いちばん長く見つめる場所です。

### カードの4色（面と文字の対）

白抜き文字を のせたときの実測。**あお・みどりは 大きな文字の基準（3:1）にも
届いていませんでした。**

| カード | 前（面 × 文字） | 比 | 後 | 比 |
|---|---|---:|---|---:|
| あか | `rose-500` × 白 | 3.67 | `rose-600` × 白 | **4.70** |
| あお | `sky-500` × 白 | **2.77** | `sky-700` × 白 | **5.93** |
| きいろ | `amber-400` × `slate-800` | 8.76 | （変更なし） | 8.76 |
| みどり | `emerald-500` × 白 | **2.54** | `emerald-700` × 白 | **5.48** |

色相は変えていません。面を1〜2段だけ濃くしました。
きいろは 面を濃くしても白抜きにできない（`amber-400` と白は 1.67）ため、
もとから文字を濃くしてあり、そこは触っていません。

この対応は `tests/cards.test.mjs` で 自動チェックしています。
**色を変えると テストが落ちます。**

### タップ領域 44px

4つの要素が 44px に届いていませんでした。いずれも **疑似要素（`.tap-44::after`）で
当たり判定だけを広げて**います。`min-height` を当てるとヘッダーやツールバーの行が
折り返し、別の破綻を生むためです（§2-9）。

| 要素 | 前 | 後 |
|---|---|---|
| ヘッダーのロゴ（さいしょへ もどる） | 161.4 × **28**（幅により 175.3 × **28**） | 当たり判定 44 以上 |
| ⚙ せってい | **40 × 40** | 当たり判定 44 以上 |
| フッターの「GIGA山」 | 53 × **20** | 当たり判定 44 以上 |
| 「やめる」（れんしゅう中） | 91 × **38** | 当たり判定 44 以上 |

---

## 2. PWA の挙動（実測。§7-5）

`npm run measure` が 実ブラウザで 次を確かめています。**すべて 0件（＝正常）**。

| 確かめたこと | やり方 | 結果 |
|---|---|---|
| Service Worker が登録されているか | `getRegistration()` | ✅ active |
| **初回訪問で 勝手にリロードしないか** | まっさらな状態で1回開き `framenavigated` を数える | ✅ **1回** |
| 先読みに 本体が入っているか | キャッシュの中身を列挙 | ✅ index/offline/app/react/css すべて |
| **押すまで 切りかわらないか** | 版を上げて `update()` → **3秒放置** | ✅ `waiting` のまま |
| 更新のおしらせが 出るか | 画面を見る | ✅「あたらしい ばんが あります」 |
| **押したら 切りかわるか** | 「さいしんに する」を押す | ✅ `waiting` が消える |
| **他アプリを 巻きぞえにしないか** | 別名のキャッシュを2つ置いて 版を上げる | ✅ 2つとも残った |
| **圏外で 起動するか** | サーバーを止めて 開き直す | ✅ 起動する |
| `offline.html` が 出るか | 本体のキャッシュだけ消してから 圏外に | ✅ 出る |

### 直したこと

| 前 | 後 |
|---|---|
| `install` の中で `skipWaiting()` していた（操作中に画面が入れかわる） | 外した。`message` で言われたときだけ切りかえる |
| 更新のおしらせが **無かった**（新版が来ても 児童は気づけない） | 画面下に おしらせを出す |
| `controllerchange` の受け口が 無かった | **利用者が押したときだけ**受ける（初回の勝手なリロードを防ぐ） |
| SW 登録が `addEventListener('load')` だけ | `readyState` の分岐を入れた（§3-6） |
| `offline.html` が `<script>` に頼っていた | `<a href>` に変えた。CSP でも動く（§3-4） |
| 先読みに `App.jsx`（翻訳前）を入れていた | ビルド後のファイル一式に |
| `VERSION` `v1.3.0` | **`v1.4.0`**（`APP_VERSION` と `package.json` もそろえた） |

> ### ⚠️ 実測の道具そのものに 不具合が2件ありました
>
> 「わざと壊して確かめる」をしていなければ、どちらも **「異常なし」と
> 報告してしまうところ**でした。記録しておきます。
>
> 1. **`context.setOffline(true)` は 圏外を作れていなかった。**
>    Playwright の route（通信の横取り）を入れていると、`setOffline` を
>    すり抜けて 通信が成功する。「圏外で起動した ✅」は でたらめだった。
>    → **サーバーそのものを止める**方式に変えた。
> 2. **先読みの確認が 他アプリのキャッシュを見ていた。**
>    「`core` を含むキャッシュ」で探していたため、巻きぞえ確認用に置いた
>    `hiragana-master-core-v1` を つかんで「先読みに何も入っていない」と
>    報告した。→ **自アプリの接頭辞で絞る**ように直した。

### maskable アイコン（§3-7）

「欠け」ではなく **「余白」**のパターンでした。角丸四角の下地が 端まで届いておらず、
円で切り抜かれると 内側が余白色で埋まり **縮んで見える**状態です。

| | 前 | 後 |
|---|---|---|
| 下地 | 角丸四角（512 中 70〜438px）。外は白 | **端まで のばした** |
| セーフゾーン外の「中身」 | 0.00%（欠けてはいなかった） | **0.00%** |
| 512 のサイズ | 9.9 KB | 24.9 KB |

やり方は `tools/make-maskable.mjs` にあります。元の下地は 左上が明るく右下が暗い
グラデーションなので、**単色を敷くと 角丸四角の輪郭が薄い影として残ります。**
そこで、タイルのふちの帯（絵が写っていない部分）だけから 1次式で
グラデーションを あてはめ、それを 全面に のばしました（あてはめの最大のずれ 14.5/255）。

> **数え方の注意（§3-7）。** 下地の式からの ずれで数えると 0.26% になりますが、
> それは **グラデーションが完全な1次式ではないぶん**であって 中身ではありません。
> 「色がついている／目に見えて暗い」画素だけを 中身として数えると **0画素**です。
> 下地と中身を 色で区別しないと、実態より深刻に見えます。

---

## 3. 監査表（A〜G）

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | 新規作成（MIT / Copyright (c) 2026 GIGAyama） |
| A2 | .gitignore | ✅ | 新規作成。`node_modules/` `.env` `.clasp.json` ほか |
| A3 | dependabot.yml | ✅ | 新規作成。monthly・メジャー更新は自動で上げない |
| A4 | README / MANUAL / AUDIT | ✅ | README 更新、MANUAL・AUDIT 新規 |
| A5 | CI が `pull_request` でも動く | ✅ | `.github/workflows/ci.yml` 新規。push と pull_request の両方 |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで **動作確認ずみ**） | ✅ | `index.html` / `offline.html` に投入。39通りの画面で **CSP 違反 0件・JS エラー 0件** |
| B2 | 秘密情報・IDの直書きなし | ✅ | 静的検査 0件。もともと通信も個人情報も無し |
| B3 | OAuthスコープ最小 | — | 対象外（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` は SW への `{type:'SKIP_WAITING'}` のみ（宛先指定なし） |
| B5 | サーバー側5段ガード | — | 対象外（サーバーを持たない・個人情報を扱わない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | **0 バイト**（前：3本・3.3MB以上） |
| B7 | 残る外部資産に SRI と版の固定 | — | 対象外。**外部資産は Google Fonts だけ**で、これは「見た目だけ」の依存（§2-7）。届かなくても端末側フォントに落ちる |

> `frame-ancestors` は **書いていません。** `<meta>` では無視され、
> 読み込みのたびに警告が出るだけになるためです。HTTP ヘッダーが要りますが
> GitHub Pages では足せません。`index.html` にその旨をコメントで残しています。

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 対象外（GAS を使っていない） |
| C2 | 自動復旧 | ✅ | 学習ログが壊れていたら 空から やり直す（`studyLog.js`）。改修前から実装ずみ |
| C3 | pagehide で記録確定 | ✅ | 実装ずみ（改修前から） |
| C4 | 通信失敗時のリトライと明示 | ✅ | 通信をしない。圏外は `offline.html` で明示 |
| C5 | `localStorage.clear()` を使っていない | ✅ | 使用なし。「きろくを すべて けす」は `keisan-card-` の3キーだけ |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に `viewport-fit=cover` | ✅ | `index.html` / `offline.html` の両方 |
| D2 | `100dvh` を使用（`100vh` 単独でない） | ✅ | `@supports not (height: 100dvh)` でフォールバック |
| D3 | safe-area-inset を適用 | ✅ | 上下に加えて **左右**も（`.app-shell`）。横向き時のノッチ欠け対策 |
| D4 | `clamp()` による fluid type | ✅ | 見出し・説明文・カードの式／こたえ・クリアタイム |
| D5 | Canvas の DPR 補正 | — | 対象外（Canvas を使っていない。絵はすべてインライン SVG） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **13画面すべてで 0件** |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` は 1つも無し。アイコン最大 24.9 KB（合計 59.9 KB） |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **39通りを走査して 0件**（前：110件） |
| D9 | **タップ領域 44px 以上** | ✅ | **`::after` 込みで実測して 0件**（前：112件） |
| D10 | prefers-reduced-motion（`.01ms` であって 0 でない） | ✅ | `.01ms`。0 にすると `fill-mode: forwards` が壊れ、`animate-rise` の要素が消える |
| D11 | forced-colors 対応 | ✅ | 新規追加（ボタン・リンク・ラベルに輪郭） |
| D12 | 提示モード | — | 対象外（一斉提示ではなく 児童ひとりずつの練習。カードの式は `clamp(2.75rem, 13vmin, 8rem)` で画面いっぱいに出る） |
| D13 | 印刷CSS | — | 対象外（印刷して配るアプリではない） |
| D14 | **拡大を禁止していない** | ✅ | `user-scalable=no` / `maximum-scale=1.0` を **削除**。誤ズームは `touch-action` で防ぐ |

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | `/Keisan-Card/`（改修前から正しい） |
| E2 | アイコン4種 + **透明を含まない** apple-touch-icon | ✅ | `apple-touch-icon.png` の透明画素 **0.00%**（画素を数えて確認） |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `install-hook.js`。`<head>` の最初の script |
| E4 | インストールボタン | ✅ | 実装ずみ。出せないときは 端末別の手順を案内 |
| E5 | 自アプリ接頭辞のキャッシュのみ削除（**実測で確認**） | ✅ | 別名のキャッシュ2つを置いて版を上げ、**2つとも残った** |
| E6 | sw.js が localStorage に触れていない | ✅ | 使用なし |
| E7 | 更新通知（押すまで切りかわらないことを実測） | ✅ | **3秒放置して `waiting` のまま**／押したら切りかわる |
| E8 | **初回訪問で勝手にリロードしない** | ✅ | **画面遷移 1回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | `getRegistration()` で active を確認 |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | `<script>` を撤去し `<a href>` に。CSP `script-src 'none'` |
| E11 | APP_VERSION を今回のリリース値に更新 | ✅ | `v1.4.0`（`sw.js` / `src/App.jsx` / `package.json` をそろえた） |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **0.00%**（前も 0.00%。直したのは「縮んで見える」ほう） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | `MANUAL.md` §2 |

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | `<img>` なし。アイコンのみのボタンに `aria-label`、すすみ具合とクリアに `aria-live="polite"` |
| F2 | モーダルに `role="dialog"`・Esc で閉じる | ✅ | 2つのモーダルに `role="dialog" aria-modal="true"`。Esc は **既存の「もどる」と同じ `onClose` に繋いだ**ので挙動が一致する。フォーカスも閉じこめ、閉じたら元のボタンへ戻す |
| F3 | キーボードのみで全機能に到達 | ⚠️ **一部未計測** | スペース／Enter／←／→ でのれんしゅうは実測ずみ。**Tab 順が視覚順と一致しているかは 未計測**（自動では測れないため、人の目で見てほしい） |
| F4 | `rt` の色を決め打ちしていない | — | 対象外（ふりがなを使っていない。全文が ひらがな） |
| F5 | 初回JS 300KB以下 | ✅ | **205.9 KB**（スタイル・HTML を入れても 246.5 KB） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大 `src/App.jsx` 2,588行・約 100 KB |

### G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | 改修前から実装ずみ。今回は触っていない |
| G2 | 中断記録・5分ルール | ✅ | 改修前から実装ずみ（5分・`pagehide`・離席時刻で締める） |

---

## 4. 測っていないもの（**必ず読んでください**）

| 何を | なぜ |
|---|---|
| **本番（`gigayama.github.io`）での動作** | この作業環境から到達できません（プロキシ 403）。**GitHub Pages に出したあと、実機で1回 確かめてください** |
| **実機（Chromebook・iPad）での表示** | 手元にありません。測ったのは デスクトップ Chromium を3つの画面幅にしたものです |
| **Tailwind CDN の転送量（改修前）** | `cdn.tailwindcss.com` へ出られず、実バイトを取得できませんでした |
| **iOS Safari での PWA の挙動** | Chromium しかありません。ホーム画面追加・apple-touch-icon の見え方は 実機で確認してください |
| **Tab 順が視覚順と一致しているか** | 自動では測れません |
| **LCP・CLS の実測値** | 実機で測っていません。読むバイト数は 3.3MB 以上 → 246.5 KB に減っています |
| **フォントが当たったときの見た目** | 測定は Google Fonts を遮った状態（＝端末側フォント）で行いました |

---

## 5. 人間に決めてほしいこと

1. **`src/` へのファイル移動と ビルドの導入を受け入れるか。**
   `App.jsx` → `src/App.jsx` に移し、`npm run build` で `js/app.js` /
   `css/style.css` / `vendor/` を作る形にしました。
   アーキテクチャの変更なので、**マージは人間に委ねます**（Part III 停止条件）。
   受け入れる場合、以後の直しは **`src/` を直して `npm run build`** です。

2. **カードの4色を1〜2段 濃くしたことを 受け入れるか。**
   あお・みどりは 白抜き文字が 大きな文字の基準にも届いていなかったため
   直すことが仕様（Part III の但し書き）と判断しました。色相は変えていません。
   実物の計算カードとの色合わせを 優先したい場合は、
   **面はそのままにして 文字を `slate-800` にする**という別解もあります
   （`sky-500` × `slate-800` = 5.28、`emerald-500` × `slate-800` = 5.77）。
   きいろカードが すでにその形です。どちらが良いか 決めてください。

3. **`src/App.jsx` は 2,588行あります。** 分割は **していません**
   （Part III P3「巨大ファイルの分割は自動でやらない」）。
   分割するなら、次のような区切りが自然です。合意いただければ 1機能ずつ PR にします。
   - `src/cards.js`（4種類のカードを作る・`factKey`・`formatTime`）
   - `src/daily.js`（れんぞく記録・カレンダーの計算）
   - `src/hooks.js`（`useLocalStorage` / `useCardDeck` / `useSwipe` / `useSystemBack`）
   - `src/screens/*.jsx`（画面ごと）
   - 🆕 分割後は **ESLint の `no-undef` を必ず通すこと。** import 漏れは
     ビルドを通過して 実行時に落ちます。

4. **`studyLog.js` の正本をどこに置くか**（フリート共通の未決事項）。
   このリポジトリのものは **今回 触っていません**。

5. **フリート共通の品質ゲート正本（`scripts/lib/project-quality.mjs`）は
   このリポジトリに置いていません。** Part I の検査は
   `scripts/lib/giga-v5-checks.mjs` に分けてあるので、正本が来たら
   `scripts/check-project.mjs` で合成できます。

---

## 6. 横断で効く知見（ほかのリポジトリにも書いてください）

1. **`context.setOffline(true)` は、通信の横取り（route）と併用すると 圏外にならない。**
   「圏外で起動する ✅」が でたらめになる。**サーバーを止めて測ること。**
2. **キャッシュを「名前に core を含むもの」で探すと、他アプリのキャッシュを つかむ。**
   巻きぞえ確認のために わざと他アプリのキャッシュを置くので、なおさら当たる。
   **自アプリの接頭辞で 絞ること。**
3. **`install` ブロックの `skipWaiting` を 正規表現の「改行 } );」で区切って探すと 見のがす。**
   かっこの数を数えて 本文を取り出すこと（`listenerBody()`）。
   これは **わざと壊す確認をして 初めて見つかった。**
4. **`<head>` の `<script>` を数えるとき、コメントを落とさないと 誤検知する。**
   「あとから `<script>` を書き足すと動かない」という注意書きに反応した。
   `SW_LOCALSTORAGE` の誤検知と 同じ形。**判定の前に コメントを落とす。**
5. **maskable の「中身」を 下地の式からのずれで数えると、下地まで数えてしまう。**
   色（彩度・暗さ）で 区別すること。0.26% と 0.00% ほどの差が出る。
6. **`text-slate-400` は 白地で 2.56、`slate-500` でも `#f6f7f9` の上では 4.44 で届かない。**
   本文背景が 真っ白でないリポジトリでは、**`slate-600` まで濃くする**のが安全。
7. **Tailwind の `*-500` 系は 白抜き文字に耐えない。** 実測で
   `rose-500` 3.67 / `sky-500` 2.77 / `emerald-500` 2.54 / `amber-500` 2.15。
   **`amber` は 白抜きを あきらめて 文字を濃くするしかない。**
